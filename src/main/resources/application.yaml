spring:
  application:
    name: shopping

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cyci?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: kr.soft.shopping.dto #DTO위치
  configuration:
    map-underscore-to-camel-case: true # user_id -> userID 자동 매핑